steps:
  - name: gcr.io/cloud-builders/gcloud
    entrypoint: bash
    args:
      - -ceu
      - |
        echo "build job"
        echo "_PIPELINE=${_PIPELINE}"
options:
  pubsubTopic: projects/logical-craft-441308-v0/topics/build-status-topic
  logging: CLOUD_LOGGING_ONLY
